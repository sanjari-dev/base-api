const path = require("path");
const app = "user";
module.exports = {
  "config": path.resolve(`./database/postgres/${app}/config`, "config.cjs"),
  "models-path": path.resolve(`./database/postgres/${app}/models`),
  "seeders-path": path.resolve(`./database/postgres/${app}/seeders`),
  "migrations-path": path.resolve(`./database/postgres/${app}/migrations`)
}